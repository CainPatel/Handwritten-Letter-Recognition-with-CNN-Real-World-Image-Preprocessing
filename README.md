# Handwritten-Letter-Recognition-with-CNN-Real-World-Image-Preprocessing
A convolutional neural network that classifies handwritten letters from uploaded photos, built with PyTorch and a custom OpenCV preprocessing pipeline that handles real-world images (shadows, lighting, framing). Includes a Streamlit web interface.
